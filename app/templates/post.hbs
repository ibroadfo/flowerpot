{{#each flashMessages.queue as |flash|}}
  {{#flash-message flash=flash as |component flash|}}
    <span id='newPostHeader'>{{flash.message}}</span>
  {{/flash-message}}
{{/each}}
{{post-post model=model}}
